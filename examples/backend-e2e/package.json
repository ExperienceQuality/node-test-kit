{
  "name": "@xq/node-test-kit-demo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "engines": { "node": ">=24" },
  "dependencies": { "node-test-kit": "0.1.0" },
  "devDependencies": { "vitest": "^4.0.0" }
}
