# hub-stage: test
name: Test

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: test-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    name: Test package
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with:
          node-version: 24
          cache: npm
      - name: Install dependencies
        run: npm ci
      - name: Type-check workspaces
        run: npm run check
      - name: Run unit and integration tests
        run: npm test
      - name: Start backend E2E dependencies
        run: docker compose -f showcase/backend-e2e/docker-compose.yml up -d --wait
      - name: Run backend functional E2E tests
        run: npm run test:e2e
      - name: Stop backend E2E dependencies
        if: always()
        run: docker compose -f showcase/backend-e2e/docker-compose.yml down --volumes
      - name: Verify packed-package consumer
        run: npm run verify:packages
